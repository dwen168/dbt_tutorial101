dbt_101_profile:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: postgres
      password: mysecretpassword
      port: 5432
      dbname: postgres
      schema: raw
      threads: 4
      keepalives_idle: 0
