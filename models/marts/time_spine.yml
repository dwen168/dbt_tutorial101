version: 2

models:
  - name: time_spine
    description: "Time spine for the semantic layer"
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: "Date in day granularity"
        granularity: day